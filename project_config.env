# 專案全域設定 (360 Dense Pipeline)

# [建模視角 FOV]
# 360 拆圖時的單張視角。Dense 模式(8視角)建議用 100.0 以獲得最佳重疊率。
FOV_MODEL=100.0

# [定位視角 FOV]
# 這是給 run_localization.py 用的，定義 Query 影像的視角
FOV_QUERY=70.0

# [特徵提取模型]
# 推薦: megaloc (光照變化強健) 或 netvlad
GLOBAL_CONF="megaloc"

# ==========================================
# [幾何配對策略 (Directional Window Strategy)]
# ==========================================

# 1. Axial Window (軸向): F, B
WINDOW_AXIAL=3

# 2. Diagonal Window (對角): FR, FL, BR, BL
WINDOW_DIAGONAL=2

# 3. Lateral Window (側向): L, R
# 建議設為 1 (允許與正隔壁連接)
WINDOW_LATERAL=0

# [幀內配對 (Intra-frame)]
INTRA_MAX_ANGLE=90.0

# [Cross-Over Strategy (輸送帶流動策略)]
# 針對長廊與移動場景優化 (e.g., F_t -> LF_t+1)
# 1 = Enable (開啟), 0 = Disable (關閉)
ENABLE_CROSS_OVER=0
# 專案全域設定 (360 Dense Pipeline)

# [建模視角 FOV]
# 360 拆圖時的單張視角。Dense 模式(8視角)建議用 100.0 以獲得最佳重疊率。
FOV_MODEL=100.0

# [定位視角 FOV]
# 這是給 run_localization.py 用的，定義 Query 影像的視角
FOV_QUERY=70.0

# [特徵提取模型]
# 推薦: megaloc (光照變化強健) 或 netvlad
GLOBAL_CONF="megaloc"

# ==========================================
# [幾何配對策略 (Directional Window Strategy)]
# ==========================================

# 1. Axial Window (軸向): F, B
# 負責主要的縱向位移 (Spine)。數值越大，前後關聯越強。
WINDOW_AXIAL=3

# 2. Diagonal Window (對角): FR, FL, BR, BL
# 輔助視角。設為 -1 代表自動設為 Axial 的一半 (例如 3 -> 1)。
WINDOW_DIAGONAL=2

# 3. Lateral Window (側向): L, R
# 設為 1: 允許連接正隔壁一幀 (保持連續性)
# 設為 0: 完全禁止幀間連接 (只依賴 Intra-frame)
WINDOW_LATERAL=1

# [幀內配對 (Intra-frame)]
# 限制同一張全景圖內不同視角的連接角度。
# 設為 90 (建議值): 允許 F-R (90度) 直接連接，結構剛性強。
INTRA_MAX_ANGLE=90.0
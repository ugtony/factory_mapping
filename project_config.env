# 專案全域設定 (Factory Mapping Configuration)
# 這些數值將作為 scripts 的預設值

# 攝影機模式: std (一般) 或 360 (全景)
MODE="360"

# 360 模式專用: 1 = 啟用 (8視角), 0 = 停用 (4視角)
DENSE=1

# [建模設定]
# 360 模式拆圖時的單張視角 (建模用)
# 建議: Dense 模式用 100.0, Sparse 模式用 120.0
FOV_MODEL=100.0

# [定位設定]
# Query 影像的視角 (定位用)
# 手機一般約 70~75 度, 廣角鏡頭可能 100+
FOV_QUERY=70.0

FOV=70.0

# 全域特徵模型: netvlad, megaloc, dino_v2 等
GLOBAL_CONF="megaloc"